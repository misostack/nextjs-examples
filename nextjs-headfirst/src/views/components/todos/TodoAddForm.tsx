import { TodoModel } from "@/models/todo";
import React from "react";
interface TodoAddFormProps {
  todos: TodoModel[];
}
const TodoAddForm = (props: TodoAddFormProps) => {
  return <div>TodoAddForm</div>;
};

export default TodoAddForm;
